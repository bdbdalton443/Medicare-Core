@using AweCoreDemo.Controllers.Awesome.Lookup
@model AweCoreDemo.ViewModels.Input.LookupDemoCfgInput
<div class="efield">
    <div class="elabel">
        Pick a meal:
    </div>
    <div class="einput">
    @*begin*@
        @(Html.Awe().LookupFor(o => o.Meal)
            .Controller<MealLookupController>()
            .ClearButton(Model.ClearButton)
            .HighlightChange(Model.HighlightChange)
            .Fullscreen(Model.Fullscreen))
        @*end*@
    </div>
</div>