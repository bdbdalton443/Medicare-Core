@page
@model DemoHms.Pages.DashboardEmployeeGroups.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Root.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EmployeeGroup[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmployeeGroup[0].Groupcode)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.EmployeeGroup) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Groupcode)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.EmployeeGroupID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.EmployeeGroupID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.EmployeeGroupID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
