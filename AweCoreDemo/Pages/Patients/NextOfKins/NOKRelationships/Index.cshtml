@page
@model AweCoreDemo.Pages.Patients.NextOfKins.NOKRelationships.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Home.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NOKRelationship[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NOKRelationship[0].Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.NOKRelationship) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.NOKRelationshipID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.NOKRelationshipID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.NOKRelationshipID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
