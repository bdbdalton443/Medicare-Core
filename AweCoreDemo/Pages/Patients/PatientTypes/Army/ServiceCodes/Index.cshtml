@page
@model DemoHms.Pages.Patients.PatientTypes.Army.ServiceCodes.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Home.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ArmyService[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ArmyService[0].Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ArmyService) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ArmyServiceID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ArmyServiceID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ArmyServiceID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
