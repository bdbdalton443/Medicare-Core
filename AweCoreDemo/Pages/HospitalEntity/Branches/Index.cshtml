@page
@model DemoHms.Pages.HospitalEntity.Branches.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Root.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Branch[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Branch[0].Location)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Branch[0].HospitalID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Branch[0].BranchType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Branch) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Location)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HospitalID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BranchType.BranchTyeID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.BranchID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.BranchID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.BranchID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
