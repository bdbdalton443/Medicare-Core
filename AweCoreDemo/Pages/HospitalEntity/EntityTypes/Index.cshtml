@page
@model DemoHms.Pages.HospitalEntity.EntityTypes.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Root.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EntityType[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EntityType[0].Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.EntityType) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.EntityTypeID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.EntityTypeID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.EntityTypeID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
