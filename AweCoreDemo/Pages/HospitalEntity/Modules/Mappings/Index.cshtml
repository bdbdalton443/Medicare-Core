@page
@model DemoHms.Pages.HospitalEntity.Modules.Mappings.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Root.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Mapping[0].Hospital)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Mapping[0].Module)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Mapping) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Hospital.HospitalID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Module.ModuleID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.MappingID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.MappingID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.MappingID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
