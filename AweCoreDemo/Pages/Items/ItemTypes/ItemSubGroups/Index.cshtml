@page
@model DemoHms.Pages.Items.ItemTypes.ItemSubGroups.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Home.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ItemSubGroup[0].Code)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ItemSubGroup[0].Name)
            </th>
            <th>
                Type
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ItemSubGroup) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Code)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ItemSuperType.Name)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ItemSubGroupID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ItemSubGroupID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ItemSubGroupID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
